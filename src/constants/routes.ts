export enum AUTH_ROUTES {
    SignIn = 'SignIn',
    SignUp = 'SignUp'
}

export enum BOTTOM_ROUTES {
    Posts = 'Posts',
    Profile = 'Profile'
}

export enum APP_ROUTES {
    Auth = 'Auth',
    Home = 'Home'
}

export enum HOME_ROUTES {
    Tabs = 'Tabs'
}
